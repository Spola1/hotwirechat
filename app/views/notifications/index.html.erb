<% if current_user.present? %>
  <%= turbo_frame_tag "notifications" do %>
    <h1 class="font-bold text-4xl mb-2">Notifications</h1>
    <ul id="notifications-list">
      <%= render @notifications %>
    </ul>
  <% end %>
<% end %>